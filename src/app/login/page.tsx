import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Login - Sports Tracker",
  description: "Sign in to your account",
};

export default function Login () {
    return (
        <div>
            Login Section
        </div>
    )
}