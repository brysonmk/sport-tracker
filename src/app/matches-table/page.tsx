import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Matches Table - Sports Tracker",
  description: "View all matches and results",
};

export default function MatchesTable () {
    return (
        <div>
            Matches Table Section
        </div>
    )
}