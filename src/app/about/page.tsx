import { Metadata } from "next";

export const metadata: Metadata = {
  title: "About - Sports Tracker",
  description: "Learn more about Sports Tracker",
};

export default function About () {
    return (
        <div>
            About Section
        </div>
    )
}